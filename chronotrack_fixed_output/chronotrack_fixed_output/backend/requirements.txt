# Existing dependencies — keep unchanged
fastapi==0.111.0
uvicorn[standard]==0.29.0
motor==3.4.0
pymongo==4.7.2
PyJWT==2.8.0
bcrypt==4.1.3
python-dotenv==1.0.1
pydantic==2.10.6

# NEW — Email integration
# smtplib is part of Python stdlib; no extra install needed.

# NEW — Optional: Scheduled end-of-day emails
# Only required if ENABLE_EMAIL_SCHEDULER=true
apscheduler==3.10.4
